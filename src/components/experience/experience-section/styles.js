import styled from 'styled-components';
import Colors from '../../../styles/Colors';

export const ExperienceSectionHeader = styled.h3`
    font-weight: 300;
    color: ${Colors.white};
    font-size: 18pt;

    @media (min-width: 800px) {
        font-size: 22pt;
    }
`;
